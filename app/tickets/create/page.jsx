import CreateForm from "./CreateForm"

export default function CreateTicket() {
    return (
        <main>
            <h2 className="text-orimary text-center">
                Add a new ticket
            </h2>
            <CreateForm />
        </main>
    )
}
